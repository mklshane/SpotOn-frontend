import { ScreenPlaceholder } from '@/components/ui/screen-placeholder';

export default function LearnScreen() {
  return (
    <ScreenPlaceholder
      icon="book.fill"
      title="Learn"
      subtitle="Understand skin cancer signs, the ABCDEs, and when to see a doctor."
    />
  );
}
