import { ScreenPlaceholder } from '@/components/ui/screen-placeholder';

export default function ScanScreen() {
  return (
    <ScreenPlaceholder
      icon="camera.fill"
      title="Scan a lesion"
      subtitle="Point your camera at a spot for an instant, on-device triage."
    />
  );
}
